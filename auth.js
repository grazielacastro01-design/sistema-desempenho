function verificarAutenticacao() {
    // Escreve no console apenas para sabermos que passou por aqui
    console.log("Modo de teste: Trava de Admin desativada temporariamente.");
    
    // Deixamos a função vazia, sem dar alert e sem dar o window.location.href!
}

// Executa a função automaticamente
window.addEventListener('DOMContentLoaded', verificarAutenticacao);